import UIKit

public enum Assets {
  public enum Images {
    public static let picture = UIImage(named: Assets.ImagesNames.pictureName)
  }

  public enum ImagesNames {
    public static let pictureName = "picture"
  }
}
